apiVersion: apps/v1
kind: Deployment
metadata:
  name: deal-service
  namespace: realty
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deal-service
  template:
    metadata:
      labels:
        app: deal-service
    spec:
      containers:
      - name: deal-service
        image: 192.168.49.1:5000/realty/deal-service:latest
        envFrom:
        - configMapRef:
            name: realty-config
        - secretRef:
            name: realty-secrets
        ports:
        - containerPort: 50053
          name: grpc
        readinessProbe:
          tcpSocket:
            port: 50053
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "300m"
---
apiVersion: v1
kind: Service
metadata:
  name: deal-service
  namespace: realty
spec:
  selector:
    app: deal-service
  ports:
  - name: grpc
    port: 50053
    targetPort: 50053
