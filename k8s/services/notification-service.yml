apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: realty
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
      - name: notification-service
        image: localhost:5000/realty/notification-service:latest
        envFrom:
        - configMapRef:
            name: realty-config
        - secretRef:
            name: realty-secrets
        ports:
        - containerPort: 50055
          name: grpc
        - containerPort: 8085
          name: http
        readinessProbe:
          httpGet:
            path: /health
            port: 8085
          initialDelaySeconds: 20
          periodSeconds: 10
          failureThreshold: 5
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "300m"
        restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: realty
spec:
  selector:
    app: notification-service
  ports:
  - name: grpc
    port: 50055
    targetPort: 50055
  - name: http
    port: 8085
    targetPort: 8085
