apiVersion: apps/v1
kind: Deployment
metadata:
  name: listing-service
  namespace: realty
spec:
  replicas: 1
  selector:
    matchLabels:
      app: listing-service
  template:
    metadata:
      labels:
        app: listing-service
    spec:
      containers:
      - name: listing-service
        image: 192.168.49.1:5000/realty/listing-service:latest
        envFrom:
        - configMapRef:
            name: realty-config
        - secretRef:
            name: realty-secrets
        ports:
        - containerPort: 50052
          name: grpc
        readinessProbe:
          tcpSocket:
            port: 50052
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "300m"
---
apiVersion: v1
kind: Service
metadata:
  name: listing-service
  namespace: realty
spec:
  selector:
    app: listing-service
  ports:
  - name: grpc
    port: 50052
    targetPort: 50052
