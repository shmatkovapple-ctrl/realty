apiVersion: apps/v1
kind: Deployment
metadata:
  name: listing-service
  namespace: realty
spec:
  replicas: 1
  selector:
    matchLabels:
      app: listing-service
  template:
    metadata:
      labels:
        app: listing-service
    spec:
      containers:
      - name: listing-service
        image: localhost:5000/realty/listing-service:latest
        envFrom:
        - configMapRef:
            name: realty-config
        - secretRef:
            name: realty-secrets
        ports:
        - containerPort: 50052
          name: grpc
        - containerPort: 8082
          name: http
        readinessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 20
          periodSeconds: 10
          failureThreshold: 5
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "300m"
        restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: listing-service
  namespace: realty
spec:
  selector:
    app: listing-service
  ports:
  - name: grpc
    port: 50052
    targetPort: 50052
  - name: http
    port: 8082
    targetPort: 8082
